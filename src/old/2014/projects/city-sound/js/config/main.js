// debugging via console
var DEBUG = true;

// application language
var APPLICATION_LANGUAGE = 'en';

// check app language
var isEnglishVersionOfApp = function() {
	if (APPLICATION_LANGUAGE == 'en') {
		return true;
	} else {
		return false;
	}
}